// Arrow Functions – An Overview
// What are Arrow Functions?
// Provide a shorter syntax for writing functions with implicit returns and lexically bound this.

// Basic Syntax Comparison:

// Traditional Function Expression:
const add = function (a, b) {
  return a + b;
};

// Arrow Function:
const add = (a, b) => a + b;

// Benefits:
// Concise syntax.
// No need to bind this manually.
